{
  "private": true,
  "npmClient": "npm",
  "command": {
    "publish": {
      "ignoreChanges": ["*.md"],
      "allowBranch": "master"
    },
    "publishConfig": {
      "directory": "dist"
    }
  },
  "packages": [
    "packages/command-bus",
    "packages/query-bus"
  ],
  "version": "independent",
  "workspaces": [
    "packages/command-bus",
    "packages/query-bus"
  ]
}
